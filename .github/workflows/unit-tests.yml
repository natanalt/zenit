name: Run unit tests
on: [push]
jobs:
  build-and-deploy:
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v2

      - name: 🏗️ Build
        run: cargo build
      
      - name: 📋 Test
        run: cargo test
